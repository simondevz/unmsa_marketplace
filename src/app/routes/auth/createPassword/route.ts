import { RouteObject } from "react-router-dom";
import CreatePasswordPage from "../../../pages/auth/createPassword";

const CreatePasswordRoute: RouteObject = {
  path: "createpassword",
  Component: CreatePasswordPage,
};

export default CreatePasswordRoute;
