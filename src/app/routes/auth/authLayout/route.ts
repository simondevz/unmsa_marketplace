import { RouteObject } from "react-router-dom";
import AuthLayout from "../../../pages/auth/authLayout";

const AuthIndexRoute: RouteObject = {
  index: true,
  Component: AuthLayout,
};

export default AuthIndexRoute;
